include template/page
	.left-col
		h2 Check Links
		form(action="/linkcheck/generate", method="post", enctype="multipart/form-data", name="mailnautForm", rel="linkcheck")
			fieldset
				input(type="file", name="htmlUpload", value="Upload HTML File").file-upload

				button(type="submit").launch Launch

				if (output)
					label(for="link-list") Links Discovered:
					textarea(rows=20, cols=75)#link-lis #{output}


	.right-col
		h3 How to Use
		ol
			li Upload HTML File
			li MailNaut will parse the file and look for links
			li After reading the file, MailNaut will show you the links so you can easily review them.
			li The links are displayed with the text they accompany